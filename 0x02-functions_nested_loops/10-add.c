#include "holberton.h"
/**
 * add - function that adds two integers and returns the result.
 * @a: first number input
 * @b: second number input
 * Return: a+b result
 */
int add(int a, int b)
{
	return (a + b);
}
